<%= form_for(@phone) do |f| %>
  <% if @phone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phone.errors.count, "error") %> prohibited this phone from being saved:</h2>

      <ul>
      <% @phone.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :manufacturer %><br>
    <%= f.collection_select(:manufacturer_id, Manufacturer.all, :id, :name) %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name, size: '30x1' %>
  </div>
  <div class="field">
    <%= f.label :cpu %><br>
    <%= f.text_area :cpu, size: '30x1'  %>
  </div>
  <div class="field">
    <%= f.label :screen %><br>
    <%= f.text_area :screen, size: '30x1'  %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.number_field :price %>
  </div>
  <div class="field">
    <%= f.label :battery %><br>
    <%= f.text_area :battery, size: '30x1'  %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
